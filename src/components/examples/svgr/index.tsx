import React from "react";
import { Logo } from "@components/icons";

export const SvgrExample: React.FC = () => {
    return (
        <div>
            <Logo width={32} height={32} color="black" />
        </div>
    );
};
